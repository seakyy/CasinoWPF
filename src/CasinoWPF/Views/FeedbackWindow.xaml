<Window x:Class="CasinoWPF.Views.FeedbackWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Feedback senden" Height="500" Width="600" 
        WindowStartupLocation="CenterScreen"
        Background="#F0F2F5">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="6"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="0"
                                          Padding="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="6"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>

    <Border BorderThickness="1" BorderBrush="#D1D5DB" Padding="20" CornerRadius="8" Background="White">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Feedback senden" 
                           FontSize="22" FontWeight="Bold" 
                           Foreground="#1F2937"/>
                <TextBlock Text="Teilen Sie uns Ihre Ideen und Verbesserungsvorschläge mit" 
                           FontSize="14" Foreground="#6B7280" Margin="0,5,0,0"/>
            </StackPanel>

            <!-- Feedback-Optionen -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
                <TextBlock Text="Feedback senden via:" 
                           VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#374151"/>
                <RadioButton x:Name="EmailOption" Content="E-Mail" 
                             IsChecked="True" GroupName="FeedbackType" 
                             Margin="0,0,15,0" Checked="FeedbackOption_Changed"/>
                <RadioButton x:Name="GitHubOption" Content="GitHub Issue" 
                             GroupName="FeedbackType" Checked="FeedbackOption_Changed"/>
            </StackPanel>

            <!-- GitHub Info -->
            <Border x:Name="GitHubInfo" Grid.Row="2" 
                    Background="#F3F4F6" BorderBrush="#E5E7EB" 
                    BorderThickness="1" CornerRadius="6" 
                    Padding="12" Margin="0,0,0,15" Visibility="Collapsed">
                <StackPanel>
                    <TextBlock Text="Ihr Feedback wird direkt als Issue auf GitHub erstellt." 
                               FontSize="13" TextWrapping="Wrap" Foreground="#374151"/>
                    <TextBlock Text="Sie werden zu GitHub weitergeleitet." 
                               FontSize="13" Foreground="#6B7280" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- formular -->
            <StackPanel x:Name="FormFields" Grid.Row="3" Margin="0,0,0,15">
                <TextBlock Text="Ihr Name:" FontWeight="SemiBold" Foreground="#374151"/>
                <TextBox x:Name="NameInput" Margin="0,5,0,10"/>

                <TextBlock Text="Ihre E-Mail:" FontWeight="SemiBold" Foreground="#374151"/>
                <TextBox x:Name="EmailInput" Margin="0,5,0,10"/>

                <TextBlock Text="Betreff:" FontWeight="SemiBold" Foreground="#374151"/>
                <TextBox x:Name="TitleInput" Margin="0,5,0,10"/>

                <TextBlock Text="Nachricht:" FontWeight="SemiBold" Foreground="#374151"/>
                <TextBox x:Name="MessageInput" Margin="0,5,0,10" 
                         AcceptsReturn="True" Height="120" 
                         VerticalScrollBarVisibility="Auto"
                         TextWrapping="Wrap"/>
            </StackPanel>

            <!-- Error -->
            <TextBlock x:Name="ErrorText" Grid.Row="4" 
                       Foreground="#DC2626" Margin="0,0,0,15" 
                       TextWrapping="Wrap" Visibility="Collapsed" TextAlignment="Center"/>

            <!-- Buttons -->
            <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Abbrechen" Click="CancelButton_Click"
                        Width="100" Height="38" 
                        Background="Transparent" Foreground="#4B5563"
                        BorderBrush="#D1D5DB" BorderThickness="1"
                        Margin="0,0,10,0"/>
                <Button x:Name="SubmitButton" Content="Absenden" 
                        Click="SubmitFeedback_Click"
                        Width="120" Height="38"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
